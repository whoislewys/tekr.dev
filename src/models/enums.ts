export enum Axis {
  none,
  x,
  y,
};

export enum Lean {
  none = -42,
  liberal = -1,
  conservative = 1,
};

export enum AgreementLevel {
  none = -42,
  stronglyDisagree = -2,
  disagree = -1,
  meh = 0,
  agree = 1,
  stronglyAgree = 2,
};
